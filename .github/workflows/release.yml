name: 发布到 PyPI

on:
  release:
    types: [published]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: ricsy/shared-actions/.github/actions/pypi@main
        with:
          package-dir: src
          environment: pypi
